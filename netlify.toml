[build]
  command = "npm run build"
  publish = "build"
  environment = { NODE_VERSION = "18" }

# Handle React Router / single page app routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200